import type React from "react"
import "@/app/globals.css"
import { Inter } from "next/font/google"
import { ThemeProvider } from "@/components/theme-provider"

const inter = Inter({ subsets: ["latin"] })

export const metadata = {
  title: "The Entrepreneur Couple | Domonique & Felicia",
  description:
    "Helping entrepreneur couples build wealth, scale their brands, and multiply their income—without sacrificing their relationship.",
  keywords: "entrepreneur couple, business coaching, relationship, digital wealth, CEO couple, couple wealth academy",
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <ThemeProvider attribute="class" defaultTheme="light" enableSystem disableTransitionOnChange>
          {children}
        </ThemeProvider>
      </body>
    </html>
  )
}



import './globals.css'